# WAP Golden Configuration - wax210
# Pulled from: wap-pod1
# Pull date: 20260123-102650
# Target group: wax210
#
# This file is auto-generated by playbooks/pull_wap_golden_config.yml
# Manual edits will be overwritten on next pull.
---

# Metadata about this golden config
wap_golden_config_meta:
  source_wap: "wap-pod1"
  pulled_at: "20260123-102650"
  wap_type: "wax210"

# System configuration
wap_system:
  ap_name: "POD1-WAP"
  mgmt_interface_5g: False
  mgmt_interface_2g: False

# Radio configuration (channels, power, etc.)
wap_radio:
  wifi0_channel: "auto"
  wifi1_channel: "36"

# SSID configurations
wap_ssids:
  - name: "SPEAKER"
    encryption: "sae-mixed+ccmp"
    vlan: "vlan119"
    isolation: true
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_speaker_passphrase }}"
  - name: "EnMGMT9BA434"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_enmgmt9ba434_passphrase }}"
  - name: "AP SSID"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_ap_ssid_passphrase }}"
  - name: "AP SSID_5G"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_ap_ssid_5g_passphrase }}"
  - name: "EnMGMT9BA434"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_enmgmt9ba434_passphrase }}"
  - name: "SPEAKER"
    encryption: "sae-mixed+ccmp"
    vlan: "vlan119"
    isolation: true
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_speaker_passphrase }}"
  - name: "ENMGMT9BA434"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_enmgmt9ba434_passphrase }}"
  - name: "AP SSID_5G"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_ap_ssid_5g_passphrase }}"
  - name: "JTW"
    encryption: "sae-mixed+ccmp"
    vlan: "vlan116"
    isolation: true
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_jtw_passphrase }}"
  - name: "ENMGMT9BA434"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_enmgmt9ba434_passphrase }}"
  - name: "AP SSID"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_ap_ssid_passphrase }}"
  - name: "JTW"
    encryption: "sae-mixed+ccmp"
    vlan: "vlan116"
    isolation: true
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_jtw_passphrase }}"
  - name: "VOV"
    encryption: "psk2+ccmp"
    vlan: "vlan121"
    isolation: true
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_vov_passphrase }}"
  - name: "MESH_WDS_5G"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_mesh_wds_5g_passphrase }}"
  - name: "VOV"
    encryption: "psk2+ccmp"
    vlan: "vlan121"
    isolation: true
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_vov_passphrase }}"
  - name: "MESH_WDS"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_mesh_wds_passphrase }}"
  - name: "NETGEAR9BA434_4"
    encryption: "sae-mixed+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_4_passphrase }}"
  - name: "MESH_WDS_TEMP_5G"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_mesh_wds_temp_5g_passphrase }}"
  - name: "MESH_WDS_TEMP"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: true
    band_steering: false
    radio: "2.4ghz"
    enabled: true
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_mesh_wds_temp_passphrase }}"
  - name: "NETGEAR9BA434_4"
    encryption: "sae-mixed+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_4_passphrase }}"
  - name: "NETGEAR9BA434_5"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_5_passphrase }}"
  - name: "NETGEAR9BA434_5"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_5_passphrase }}"
  - name: "NETGEAR9BA434_6"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_6_passphrase }}"
  - name: "NETGEAR9BA434_6"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_6_passphrase }}"
  - name: "NETGEAR9BA434_7"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_7_passphrase }}"
  - name: "NETGEAR9BA434_7"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_7_passphrase }}"
  - name: "NETGEAR9BA434_8"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_8_passphrase }}"
  - name: "NETGEAR9BA434_8"
    encryption: "psk2+ccmp"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear9ba434_8_passphrase }}"
  - name: "NETGEAR_GuestNetwork"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear_guestnetwork_passphrase }}"
  - name: "NETGEAR_GuestNetwork"
    encryption: "none"
    vlan: "lan"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_netgear_guestnetwork_passphrase }}"
  - name: "WAX2109BA434-CONFIG-ONLY"
    encryption: "sae-mixed+ccmp"
    vlan: "mgmt"
    isolation: false
    hidden: false
    band_steering: false
    radio: "2.4ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_wax2109ba434_config_only_passphrase }}"
  - name: "WAX2109BA434-CONFIG-ONLY"
    encryption: "sae-mixed+ccmp"
    vlan: "mgmt"
    isolation: false
    hidden: false
    band_steering: false
    radio: "5ghz"
    enabled: false
    # Note: Passphrase stored separately in vault for security
    # passphrase: "{{ wap_ssid_wax2109ba434_config_only_passphrase }}"

